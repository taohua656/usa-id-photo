name: Flutter iOS Auto Build
workflows:
  ios_release:
    environment:
      flutter: 3.41.6
    scripts:
      - script: |
          flutter clean
          flutter pub get
          flutter create --platforms ios .
      - script: flutter build ipa --release --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa